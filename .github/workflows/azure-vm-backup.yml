name: Azure VM Disk Backup

on:
  workflow_dispatch:
  schedule:
    - cron: '30 11 * * *'  # Daily at 5:00 PM IST

env:
  AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Azure CLI
        run: curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Install AzCopy
        run: |
          wget https://aka.ms/downloadazcopy-v10-linux -O azcopy.tar.gz
          tar -xvf azcopy.tar.gz
          sudo mv ./azcopy_linux_amd64_*/azcopy /usr/bin/

      - name: Load backup config
        id: load_config
        run: echo "CONFIG=$(jq -c . < vm_backup_config.json)" >> $GITHUB_ENV

      - name: Run VM backup
        run: |
          echo "$CONFIG" | jq -c '.[]' | while read vm; do
            NAME=$(echo $vm | jq -r '.name')
            DISK=$(echo $vm | jq -r '.disk_name')
            RG=$(echo $vm | jq -r '.resource_group')
            LOC=$(echo $vm | jq -r '.location')
            SUB=$(echo $vm | jq -r '.subscription_id')
            CLIENT_ID="76cd8095-15aa-4b90-b72a-9e1477f9c865"
            CLIENT_SECRET="wH58Q~4lQwiD.ROpoeKRlCJAEshGLfetnmVjraCl"
            TENANT_ID="43b2ab88-f668-409f-ba76-8db3e53453ae"
            SA=$(echo $vm | jq -r '.storage_account')
            CONTAINER=$(echo $vm | jq -r '.container_name')

            echo "🔐 Authenticating to Azure for VM: $NAME..."
            az login --service-principal \
              --username "$CLIENT_ID" \
              --password "$CLIENT_SECRET" \
              --tenant "$TENANT_ID" \
              --allow-no-subscriptions || { echo "❌ Login failed for $NAME"; exit 1; }

            az account set --subscription "$SUB" || { echo "❌ Subscription set failed for $NAME"; exit 1; }

            SNAPSHOT_NAME="snap-${NAME}-$(date +%Y%m%d%H%M%S)"
            echo "📸 Creating snapshot: $SNAPSHOT_NAME"
            az snapshot create \
              --name "$SNAPSHOT_NAME" \
              --resource-group "$RG" \
              --source "$DISK" \
              --location "$LOC" \
              --sku Standard_LRS || { echo "❌ Snapshot creation failed for $NAME"; exit 1; }

            echo "🔗 Generating snapshot SAS URL..."
            SNAPSHOT_URL=$(az snapshot grant-access \
              --resource-group "$RG" \
              --name "$SNAPSHOT_NAME" \
              --duration-in-seconds 86400 \
              --output json | jq -r '.accessSas') || { echo "❌ SAS generation failed for $NAME"; exit 1; }

            DEST_URL="https://${SA}.blob.core.windows.net/${CONTAINER}/${SNAPSHOT_NAME}.vhd"
            echo "☁️ Copying snapshot to blob using AzCopy..."
            echo "SOURCE: $SNAPSHOT_URL"
            echo "DEST:   $DEST_URL"

            azcopy copy "$SNAPSHOT_URL" "$DEST_URL" --overwrite=true || { echo "❌ AzCopy failed for $NAME"; exit 1; }

            echo "✅ Snapshot copied to blob: $DEST_URL"

            echo "🧹 Cleaning up snapshot..."
            az snapshot delete --resource-group "$RG" --name "$SNAPSHOT_NAME" || { echo "❌ Snapshot deletion failed for $NAME"; exit 1; }

            echo "✅ Backup completed for VM: $NAME"
          done
