name: Azure VM Disk Backup

on:
  workflow_dispatch:  # Manual trigger
  schedule:
    - cron: '30 11 * * *'  # Daily at 5:00 PM IST (11:30 UTC)

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Azure CLI
        run: curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Load backup config
        id: load_config
        run: echo "CONFIG=$(jq -c . < vm_backup_config.json)" >> $GITHUB_ENV

      - name: Run VM backup
        run: |
          echo "$CONFIG" | jq -c '.[]' | while read vm; do
            NAME=$(echo $vm | jq -r '.name')
            DISK=$(echo $vm | jq -r '.disk_name')
            RG=$(echo $vm | jq -r '.resource_group')
            LOC=$(echo $vm | jq -r '.location')
            SUB=$(echo $vm | jq -r '.subscription_id')
            CLIENT_ID="76cd8095-15aa-4b90-b72a-9e1477f9c865"
            CLIENT_SECRET="wH58Q~4lQwiD.ROpoeKRlCJAEshGLfetnmVjraCl"
            TENANT_ID="43b2ab88-f668-409f-ba76-8db3e53453ae"
            SA=$(echo $vm | jq -r '.storage_account')
            CONTAINER=$(echo $vm | jq -r '.container_name')

            echo "🔐 Logging in to Azure..."
            az login --service-principal \
              --username "$CLIENT_ID" \
              --password "$CLIENT_SECRET" \
              --tenant "$TENANT_ID" \
              --allow-no-subscriptions || { echo "❌ Login failed for $NAME"; exit 1; }

            az account set --subscription "$SUB" || { echo "❌ Subscription set failed for $NAME"; exit 1; }

            SNAPSHOT_NAME="snap-${NAME}-$(date +%Y%m%d%H%M%S)"
            echo "📸 Creating snapshot: $SNAPSHOT_NAME"
            az snapshot create \
              --name "$SNAPSHOT_NAME" \
              --resource-group "$RG" \
              --source "$DISK" \
              --location "$LOC" \
              --sku Standard_LRS || { echo "❌ Snapshot creation failed for $NAME"; exit 1; }

            echo "☁️ Copying snapshot to blob..."
            SOURCE_URI="/subscriptions/$SUB/resourceGroups/$RG/providers/Microsoft.Compute/snapshots/$SNAPSHOT_NAME"
            DEST_BLOB="${SNAPSHOT_NAME}.vhd"

            az storage blob copy start \
              --account-name "$SA" \
              --destination-container "$CONTAINER" \
              --destination-blob "$DEST_BLOB" \
              --source-uri "$SOURCE_URI" \
              --auth-mode login || { echo "❌ Blob copy failed for $NAME"; exit 1; }

            echo "⏳ Waiting for blob copy to complete..."
            while [ "$(az storage blob copy show \
              --account-name "$SA" \
              --container-name "$CONTAINER" \
              --name "$DEST_BLOB" \
              --auth-mode login \
              --query 'copy.status' -o tsv)" != "success" ]; do
              sleep 10
              echo "Still copying..."
            done
            echo "✅ Blob copy completed"

            echo "🧹 Deleting snapshot..."
            az snapshot delete \
              --resource-group "$RG" \
              --name "$SNAPSHOT_NAME" || { echo "❌ Snapshot deletion failed for $NAME"; exit 1; }

            echo "✅ Backup completed for VM: $NAME"
          done
